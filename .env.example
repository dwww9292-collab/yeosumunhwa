# Supabase 연결 정보
# 대시보드 > Project Settings > API 에서 복사
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# 관리자 영역 base 경로 (여수시 지침: /admin 사용 금지)
# 운영 배포에서는 반드시 추측하기 어려운 값으로 설정하고, 이 파일에는 실제 값을 적지 말 것.
# 미설정 시 /manage 로 동작한다.
VITE_ADMIN_PATH=/manage
